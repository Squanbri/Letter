<div class="container-fluid">

  <div class="reg_page mt-5 mb-5 row">
    <div class="reg_logo ml-5 col-lg-7 justify-content-center" style="padding-left: 7%">
      <img class="img-fluid" width="80%" src="/images/registration-main.svg" alt="">
    </div>

    <div class="general_fields w-100 mx-5 mt-3 col-lg-3 p-3 p-lg-0">
      <div class="main_caption">Регистрация</div>
      <div class="alt_way">У вас уже есть аккаунт? <%= link_to 'Войти', new_user_session_path, class:'ml-2'%></div>
      <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
        <%= render "devise/shared/error_messages", resource: resource %>

        <div class="caption">E-mail</div>
        <%= f.email_field :email, class:'border log', autofocus: true, autocomplete: "email" %>

        <div class="caption">Придумайте пароль</div>
        <% if @minimum_password_length %>
          <em>(<%= @minimum_password_length %> characters minimum)</em>
        <% end %><br />
        <%= f.password_field :password, class:'border pass', autocomplete: "new-password" %>

        <div class="caption">Подтвердите пароль</div>
        <%= f.password_field :password_confirmation, class:'border pass', autocomplete: "new-password" %>

        <div class="p-0">
<!--          <input type="button" class="w-100 reg px-5 py-2" value="Зарегистрироваться" onclick="Send()">-->
          <%= f.submit  "Зарегистрироваться", class:'w-100 reg px-5 py-2' %>
        </div>
        <div class="text-center">Нажимая кнопку «Зарегистрироваться», вы даете согласие на обработку
          персональных данных и соглашаетесь с политикой конфиденциальности.</div>

        <div class="field">
          <%= f.label :password_confirmation %><br />
        </div>

      <% end %>

    </div>
  </div>
</div>